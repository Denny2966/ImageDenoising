#!/usr/bin/env python

"""
Python source code - replace this with a description of the code and write the code below this text.
"""

# vim: tabstop=8 expandtab shiftwidth=4 softtabstop=4

import sys
import numpy as np

WP = 1
WL = 1

x_arr = np.loadtxt('../Data/stripes-noise.txt')
y_arr = x_arr
i_dim, j_dim = x_arr.shape

for ind in ((0,0),(0,j_dim-1),(i_dim-1,0),(i_dim-1,j_dim-1)) :
    i, j = ind


for i in (0, i_dim - 1):
    for j in range(1, j_dim-1):


for j in (0, j_dim - 1):
    for i in range(1, i_dim-1):

for i in range(1, i_dim-1) :
    for j in range(1, j_dim) :
        
		
